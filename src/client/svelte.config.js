// Svelte 5 compiles <script lang="ts"> itself; no preprocessor needed (no
// SCSS/PostCSS). A copy lives at the repo root too so svelte-check finds one
// above shared/, which sits outside the Vite root (src/client).
export default {}