// pwa-kit: shell/svelte.config.js v6
// Svelte 5 compiles <script lang="ts"> itself; no preprocessor needed for the
// kit (no SCSS/PostCSS). svelte-check finds the nearest svelte.config.js above
// each file, so in a repo with shared/ beside the apps put a copy at the root.
export default {}